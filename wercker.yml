box: debian
build:
  steps:
    - script:
        code: |
            id
deploy:
  steps: 
    - script:
        code: |
            uname -a
            dpkg --list
            sudo apt-get install curl
    - fbrinker/appengine-deploy-path:
        email: $EMAIL
        password: $APP_PASS
        srcpath: ./appengine
