box: pypi/appengine-sdk
build:
  steps:
    - script:
        code: |
            id
deploy:
  steps: 
    - script:
        code: |
            cd $WERCKER_SOURCE_DIR
            app_cfg.py appengine/app.yaml --email $EMAIL --w

