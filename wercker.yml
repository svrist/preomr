box: google/cloud-sdk
build:
  steps:
    - script:
        code: |
            id
deploy:
  steps: 
    - script:
        code: |
            uname -a
            which dev_appserver.py
            which app_cfg.py
