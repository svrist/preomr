box: google/cloud-sdk
build:
  steps:
    - script:
        code: |
            id
deploy:
  steps: 
    - script:
        code: |
            uname -a
            which dev_appserver.py
            ls /google-cloud-sdk/bin/
            cd $WERCKER_SOURCE_DIR
            gclod preview app deploy appengine/app.yaml
