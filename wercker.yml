box: ubuntu
build:
  steps:
    - script:
        code: |
            id
deploy:
  steps: 
    - script:
        code: |
            uname -a
            dpkg --list
            sudo apt-get -y install curl 
    - fbrinker/appengine-deploy-path:
        email: $EMAIL
        password: $APP_PASS
        srcpath: ./appengine
