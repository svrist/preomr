box: debian
build:
  steps:
    - script:
        code: |
            id
deploy:
  steps: 
    - script:
        code: |
            apt-get install curl
    - fbrinker/appengine-deploy-path:
        email: $EMAIL
        password: $APP_PASS
        srcpath: ./appengine
