box: python:2.7-slim
build:
  steps:
    - script:
        code: |
            id
deploy:
  steps: 
    - fbrinker/appengine-deploy-path:
        email: $EMAIL
        password: $APP_PASS
        srcpath: ./appengine
